{
  "name": "gutu-lib-ui-zone-next",
  "private": true,
  "type": "module",
  "workspaces": [
    "framework/libraries/*"
  ],
  "scripts": {
    "build": "bun run --cwd framework/libraries/ui-zone-next build",
    "typecheck": "bun run --cwd framework/libraries/ui-zone-next typecheck",
    "lint": "bun run --cwd framework/libraries/ui-zone-next lint",
    "test": "bun run --cwd framework/libraries/ui-zone-next test",
    "test:unit": "bun run --cwd framework/libraries/ui-zone-next test:unit",
    "docs:summary": "node scripts/docs-summary.mjs",
    "docs:check": "node scripts/docs-check.mjs"
  }
}
